import * as React from 'react';
import { Button } from 'antd';

const Login = () => {
  return (
    <div>
      login <Button type="primary">Primary Button</Button>
    </div>
  );
};

export default Login;
